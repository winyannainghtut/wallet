apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-frontend
  namespace: wallet-app
  labels:
    app: wallet-frontend
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wallet-frontend
  template:
    metadata:
      labels:
        app: wallet-frontend
        tier: frontend
    spec:
      containers:
        - name: wallet-app
          image: winyannainghtut/wallet-app:dev-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: POCKETBASE_URL
              value: "http://pocketbase-service.wallet-app.svc.cluster.local:8090"
            - name: AUTH_REGISTRATION_ENABLED
              value: "false"
            - name: AUTH_ALLOWED_EMAILS
              value: ""
            - name: AUTH_ALLOWED_DOMAINS
              value: ""
            - name: AI_DEFAULT_MODEL
              value: "glm-4.7"
            - name: ZAI_OPENAI_BASE_URL
              value: "https://api.z.ai/api/coding/paas/v4"
            - name: ZAI_API_KEYS_JSON
              valueFrom:
                secretKeyRef:
                  name: wallet-ai-secrets
                  key: ZAI_API_KEYS_JSON
            - name: ZAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: wallet-ai-secrets
                  key: ZAI_API_KEY
                  optional: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1001
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: wallet-frontend-service
  namespace: wallet-app
  labels:
    app: wallet-frontend
    tier: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: wallet-frontend
