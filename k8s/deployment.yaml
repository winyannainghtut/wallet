apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-frontend
  namespace: wallet-app
  labels:
    app: wallet-frontend
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wallet-frontend
  template:
    metadata:
      labels:
        app: wallet-frontend
        tier: frontend
    spec:
      containers:
        - name: wallet-app
          image: wallet-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: NEXT_PUBLIC_POCKETBASE_URL
              value: "https://pb.yourdomain.com"
            - name: POCKETBASE_URL
              value: "http://pocketbase-service.wallet-app.svc.cluster.local:8090"
            - name: AUTH_REGISTRATION_ENABLED
              value: "false"
            - name: AUTH_ALLOWED_EMAILS
              value: ""
            - name: AUTH_ALLOWED_DOMAINS
              value: ""
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: wallet-frontend-service
  namespace: wallet-app
  labels:
    app: wallet-frontend
    tier: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: wallet-frontend
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wallet-frontend-ingress
  namespace: wallet-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: wallet.yourdomain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wallet-frontend-service
                port:
                  number: 80
