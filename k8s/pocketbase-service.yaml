apiVersion: v1
kind: Service
metadata:
  name: pocketbase-service
  namespace: wallet-app
  labels:
    app: pocketbase
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
  selector:
    app: pocketbase
